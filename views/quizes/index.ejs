
    
    
    <table>
    <tr>
    <td class="bl"><h2>PREGUNTAS</h2></td>
    <td class="bl">
    <form method="get" action="/quizes">
  <label for="resp" class="rem">Buscador</label><br>
    <input type="text" name="search" value="Texto a buscar" />
    <input value="Buscar" type="submit">
</form>
    </td>
    </tr>
    </table>

    <table>
    
    <tr >
    <td class="borderbotton">TEMA</td>
    <td class="borderbotton">PREGUNTA</td>
    <td colspan="2" class="borderbotton"></td>
    </tr>
    <tr> 
    <% var i; for (i=0; i < quizes.length; i++){%>
    <td><a href="quizes/<%= quizes[i].id%>"><%= quizes[i].pregunta %></a></td>
    <td class="ita"><%= quizes[i].tema %></td>
    <td><a href="quizes/<%= quizes[i].id%>/edit"><button>Editar</button></a></td>

     <td>
          <form method="post" action="/quizes/<%= quizes[i].id %>?_method=delete">
            <button type="submit" onClick="return confirm('¿Seguro que quieres borrar:\n <%= quizes[i].pregunta %>?');">
              borrar
            </button>
          </form>
     </td>

    </tr>
    <% }%>
    </table>
    <p> <a href="/quizes/new"><button type="button"> Crear pregunta</button></a></p>
     
   